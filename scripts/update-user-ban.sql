-- =============================================================
-- سكربت إضافة عمود الحظر إلى جدول الملفات الشخصية (profiles)
-- نفّذ هذا السكربت مرة واحدة في SQL Editor داخل داشبورد Supabase
-- =============================================================
-- القيم الممكنة في العمود:
--   NULL      : غير محظور
--   timestamp : محظور مؤقتاً حتى التاريخ المحدد (يوم / أسبوع / شهر)
--   infinity  : محظور نهائياً (قيمة مدعومة أصلاً في PostgreSQL لنوع timestamptz)
-- =============================================================

ALTER TABLE public.profiles
  ADD COLUMN IF NOT EXISTS banned_until timestamptz;
